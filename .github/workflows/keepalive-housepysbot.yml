name: keepalive-housepysbot
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot health endpoint
        run: curl -sSf -m 20 https://housepysbot.onrender.com/health > /dev/null
