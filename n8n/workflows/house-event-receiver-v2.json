{
  "name": "House Event Receiver",
  "nodes": [
    {
      "parameters": {
        "path": "house-event",
        "httpMethod": "POST",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-house-event",
      "webhookId": "webhook-house-event",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [250, 300]
    },
    {
      "parameters": {
        "jsCode": "const input = $input.first().json;\nconst body = input.body || {};\n\n// Calculate suggested order qty\nconst minStock = body.payload?.minStock || 0;\nconst currentStock = body.payload?.currentStock || 0;\nconst suggestedQty = Math.ceil(Math.max(0, (minStock - currentStock) * 1.2));\n\nreturn [{ json: {\n  received: true,\n  eventId: body.eventId,\n  type: body.type,\n  tenantId: body.tenantId,\n  source: body.source,\n  correlationId: body.correlationId,\n  productName: body.payload?.productName,\n  productId: body.payload?.productId,\n  currentStock,\n  minStock,\n  suggestedQty,\n  timestamp: new Date().toISOString()\n}}];"
      },
      "id": "code-process-event",
      "name": "Procesar Evento",
      "type": "n8n-nodes-base.code",
      "typeVersion": 1,
      "position": [450, 300]
    },
    {
      "parameters": {
        "respondWith": "firstIncomingItem",
        "options": {}
      },
      "id": "respond-event",
      "name": "Responder",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [650, 300]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [[{ "node": "Procesar Evento", "type": "main", "index": 0 }]]
    },
    "Procesar Evento": {
      "main": [[{ "node": "Responder", "type": "main", "index": 0 }]]
    }
  },
  "active": true,
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": "{}",
  "tags": []
}
